#include <stdio.h>
/* Print the lowercase alphabets*/
/**
 * main - lowercase alphabets
 * Return: 0 means Success
 */
int main(void)
{
	char th;

	for (th = 'a' ; th <= 'z' ; th++)
	{
		putchar(th);
	}
	return (0);
}
