#include "main.h"
/* A function that adds two integers */
/**
 * add - adds two integers
 * @num1: argument to be used
 * @num2: argument to be used
 * Return: the sum of the two integers
 */
int add(int num1, int num2)
{
	int b;

	b = (num1 + num2);
	return (b);
}
